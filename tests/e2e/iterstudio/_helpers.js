// Local test helpers for the iterstudio batch. Most are imports from the
// preset _helpers since the shared concerns (normalizeIterStudioSettings)
// are the same.

export { normalizeIterStudioSettings } from '../preset/_helpers.js';
